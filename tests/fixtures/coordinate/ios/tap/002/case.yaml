platform: ios
label_id: "8"
target_text: "Confirm & proceed"
description: |
  Inner static-text element nested inside the footer container button.
  XML element bounds are (140, 846, 150, 18) in logical points. After
  scale-factor x3 the manifest carries (420, 2538, 450, 54) device-pixel.
  Same clamp pathology as the footer case — y truncates to 931 and the
  width gets clipped against the screen right edge — yielding the
  spurious (425, 931) production dispatch.
screen:
  logical:
    width: 430
    height: 932
expected:
  resolved_pixel_bounds:
    x: 420
    y: 2538
    width: 450
    height: 54
  dispatched_logical:
    x: 215
    y: 855
  observed_buggy_dispatched_logical:
    x: 425
    y: 931
