"""
Architecture-governance tooling: fitness rules, the debt manifest, and reporting.

This package is repository tooling, never shipped in the ``fathom`` wheel. It is the
single source of truth consumed by both the ``python -m governance`` pre-commit hook
and the ``tests/architecture`` fitness suite.
"""
